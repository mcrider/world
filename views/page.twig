{% extends "base.twig" %}

{% block content %}
	<article class="post-type-{{post.post_type}}" id="post-{{post.ID}}">
		<h2>{{post.title}}</h2>
		<section class="article-body">
			{{post.content}}
		</section>
	</article>
{% endblock %}
